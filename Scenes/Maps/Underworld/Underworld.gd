extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
#	var destination_scene = load("res://Scenes/Maps/DeliveryDestination/DeliveryDestination.tscn")
#	var new_destination = destination_scene.instance()
#	var new_position = Vector2(rand_range(Game.player.position, Vector2(0,0)), rand_range(Game.player.position, Vector2(0,0)))
#	add_child(new_destination)
#	new_destination.set_global_position(new_position)
#
#	Game.player.current_destionation = new_destination
#
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass


